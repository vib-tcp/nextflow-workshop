// Extract all the parameters from the main.nf script and paste them in this separate params.config file. 
// Define project parameters needed for running the pipeline
params {
    outdir = "$launchDir/results"
    input = "$launchDir/data/crocodile_dataset.csv"
}
